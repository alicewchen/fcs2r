% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcs_to_median_PEA.R
\name{fcs_to_median_PEA}
\alias{fcs_to_median_PEA}
\title{Export median PE-A (FL2-A) from multiple `.fcs` files}
\usage{
fcs_to_median_PEA(folder_path_containing_fcs, output_filepath = NULL)
}
\arguments{
\item{folder_path_containing_fcs}{Path to the folder containing \code{.fcs} files}

\item{output_filepath}{(optional) File path for the output; \code{NULL} by default; must end in \code{.txt}}
}
\value{
Returns a dataframe with the 3 columns: `Well_id`,`median_PE_A`,`Plate`
}
\description{
This function calculates the median PE-A of each `.fcs` file within a folder and returns all the values as a dataframe.
}
\details{
\code{.fcs} file names must follow the following format:

\code{Plate number(##)-Well-Well position([A-Z]##)_*.fcs}

Examples:

\code{01-Well-A1.fcs}

\code{01-Well-A1_Live alga.fcs}
}
\examples{
library(fcs2r)
wd<- getwd()
fcs_folder <- paste(system.file(package = "fcs2r"),"/extdata/fcs_data/", sep ="")
df <- fcs_to_median_PEA(fcs_folder) #save output as dataframe
fcs_to_median_PEA(fcs_folder, "test.txt") #export as text file
}
