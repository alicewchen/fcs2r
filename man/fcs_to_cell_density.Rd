% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcs_to_cell_density.R
\name{fcs_to_cell_density}
\alias{fcs_to_cell_density}
\title{Export cell density from multiple \code{.fcs} files}
\usage{
fcs_to_cell_density(folder_path_containing_fcs, output_filepath = NULL)
}
\arguments{
\item{folder_path_containing_fcs}{Path to the folder containing \code{.fcs} files}

\item{output_filepath}{(optional) File path for the output; \code{NULL} by default; must end in \code{.txt}}
}
\value{
Returns a dataframe with the 3 columns: \code{Well_id}, \code{cell density},\code{Plate}
}
\description{
This function calculates the cell density of each \code{.fcs} file within a folder and returns all the values as a dataframe.
\eqn{Cell density (cells per uL) = the total events recorded/the total sample volume}.
}
\details{
\code{.fcs} file names must follow the following format:

\code{Plate number(##)-Well-Well position([A-Z]##)_*.fcs}

Examples:

\code{01-Well-A1.fcs}

\code{01-Well-A1_Live alga.fcs}
}
\examples{
library(fcs2r)
wd<- getwd()
fcs_folder <- paste(system.file(package = "fcs2r"),"/extdata/fcs_data/", sep ="")
df <- fcs_to_cell_density(fcs_folder) #save output as dataframe
fcs_to_cell_density(fcs_folder, "test.txt") #export as text file
}
